{
  "name": "gemini-sidebar-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:inject": "node ./scripts/inject-env.js",
    "build": "npm run build:inject && rm -rf dist && mkdir -p dist && cp -R public/* dist/ && tsc",
    "watch": "tsc -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "dotenv": "^17.2.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "marked": "^16.2.1"
  }
}
