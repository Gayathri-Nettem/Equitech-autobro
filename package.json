{
  "name": "gemini-sidebar-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R public/* dist/ && tsc",
    "watch": "tsc -w --preserveWatchOutput"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "marked": "^16.2.1"
  }
}
